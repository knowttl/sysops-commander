<ui:FluentWindow x:Class="SysOpsCommander.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="{Binding Title}"
        Height="700"
        Width="1100"
        WindowStartupLocation="CenterScreen"
        ExtendsContentIntoTitleBar="True"
        AutomationProperties.Name="SysOps Commander Main Window">

    <!-- Keyboard Shortcuts -->
    <ui:FluentWindow.InputBindings>
        <KeyBinding Key="F" Modifiers="Control" Command="{Binding FocusSearchCommand}" />
        <KeyBinding Key="E" Modifiers="Control" Command="{Binding NavigateToExecutionCommand}" />
        <KeyBinding Key="D" Modifiers="Control" Command="{Binding OpenDomainSelectorCommand}" />
        <KeyBinding Key="F5" Command="{Binding RefreshCurrentViewCommand}" />
        <KeyBinding Key="Escape" Command="{Binding CancelCurrentOperationCommand}" />
    </ui:FluentWindow.InputBindings>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Custom TitleBar -->
        <ui:TitleBar Grid.Row="0"
                     Title="{Binding Title}"
                     ShowMaximize="True"
                     ShowMinimize="True" />

        <!-- Main layout: sidebar + content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Sidebar Navigation -->
            <Border Grid.Column="0"
                    Width="220"
                    Padding="8,8"
                    Background="{DynamicResource ControlFillColorDefaultBrush}"
                    BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                    BorderThickness="0,0,1,0">
                <DockPanel LastChildFill="False">
                    <!-- Main nav buttons -->
                    <StackPanel DockPanel.Dock="Top">
                        <Button Command="{Binding NavigateCommand}"
                                CommandParameter="Dashboard"
                                HorizontalContentAlignment="Left"
                                Margin="0,2"
                                Padding="12,10"
                                AutomationProperties.Name="Navigate to Dashboard">
                            <StackPanel Orientation="Horizontal">
                                <ui:SymbolIcon Symbol="Home24" Margin="0,0,12,0" />
                                <TextBlock Text="Dashboard" VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>
                        <Button Command="{Binding NavigateCommand}"
                                CommandParameter="ADExplorer"
                                HorizontalContentAlignment="Left"
                                Margin="0,2"
                                Padding="12,10"
                                AutomationProperties.Name="Navigate to AD Explorer">
                            <StackPanel Orientation="Horizontal">
                                <ui:SymbolIcon Symbol="PeopleTeam24" Margin="0,0,12,0" />
                                <TextBlock Text="AD Explorer" VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>
                        <Button Command="{Binding NavigateCommand}"
                                CommandParameter="Execution"
                                HorizontalContentAlignment="Left"
                                Margin="0,2"
                                Padding="12,10"
                                AutomationProperties.Name="Navigate to Execution">
                            <StackPanel Orientation="Horizontal">
                                <ui:SymbolIcon Symbol="Play24" Margin="0,0,12,0" />
                                <TextBlock Text="Execution" VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>
                        <Button Command="{Binding NavigateCommand}"
                                CommandParameter="ScriptLibrary"
                                HorizontalContentAlignment="Left"
                                Margin="0,2"
                                Padding="12,10"
                                AutomationProperties.Name="Navigate to Script Library">
                            <StackPanel Orientation="Horizontal">
                                <ui:SymbolIcon Symbol="Code24" Margin="0,0,12,0" />
                                <TextBlock Text="Script Library" VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>
                        <Button Command="{Binding NavigateCommand}"
                                CommandParameter="AuditLog"
                                HorizontalContentAlignment="Left"
                                Margin="0,2"
                                Padding="12,10"
                                AutomationProperties.Name="Navigate to Audit Log">
                            <StackPanel Orientation="Horizontal">
                                <ui:SymbolIcon Symbol="ClipboardTextLtr24" Margin="0,0,12,0" />
                                <TextBlock Text="Audit Log" VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>
                    </StackPanel>

                    <!-- Settings at the bottom -->
                    <StackPanel DockPanel.Dock="Bottom">
                        <Separator Margin="4,8" />
                        <Button Command="{Binding NavigateCommand}"
                                CommandParameter="Settings"
                                HorizontalContentAlignment="Left"
                                Margin="0,2"
                                Padding="12,10"
                                AutomationProperties.Name="Navigate to Settings">
                            <StackPanel Orientation="Horizontal">
                                <ui:SymbolIcon Symbol="Settings24" Margin="0,0,12,0" />
                                <TextBlock Text="Settings" VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </DockPanel>
            </Border>

            <!-- Content Area -->
            <Border Grid.Column="1"
                    Margin="0"
                    Padding="16">
                <ContentControl Content="{Binding CurrentView}">
                    <ContentControl.Style>
                        <Style TargetType="ContentControl">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding CurrentView}" Value="{x:Null}">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate>
                                                <TextBlock Text="Select a view from the sidebar to get started."
                                                           Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                                           HorizontalAlignment="Center"
                                                           VerticalAlignment="Center"
                                                           FontSize="16" />
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ContentControl.Style>
                </ContentControl>
            </Border>

            <!-- Loading Overlay -->
            <Grid Grid.Column="0" Grid.ColumnSpan="2"
                  Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}"
                  Background="#80000000" Panel.ZIndex="100">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <ProgressBar IsIndeterminate="True" Width="200" Height="8" />
                    <TextBlock Text="{Binding BusyMessage}" Foreground="White"
                               HorizontalAlignment="Center" Margin="0,8,0,0" />
                </StackPanel>
            </Grid>
        </Grid>

        <!-- Status Bar -->
        <StatusBar Grid.Row="2"
                   Background="{DynamicResource ControlFillColorDefaultBrush}"
                   AutomationProperties.Name="Application Status Bar">
            <StatusBarItem>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Domain:" Margin="0,0,4,0" />
                    <ComboBox ItemsSource="{Binding AvailableDomains}"
                              SelectedItem="{Binding SelectedDomain}"
                              DisplayMemberPath="DomainName"
                              MinWidth="160"
                              AutomationProperties.Name="Domain Selector" />
                </StackPanel>
            </StatusBarItem>
            <Separator />
            <StatusBarItem>
                <StackPanel Orientation="Horizontal">
                    <ui:SymbolIcon Symbol="Person24" Margin="0,0,4,0" />
                    <TextBlock Text="{Binding CurrentUserName}" AutomationProperties.Name="Current User" />
                </StackPanel>
            </StatusBarItem>
            <Separator />
            <StatusBarItem>
                <StackPanel Orientation="Horizontal">
                    <Ellipse Width="10" Height="10"
                             Fill="{Binding ConnectionStatus, Converter={StaticResource StatusToColorConverter}}"
                             Margin="0,0,4,0" />
                    <TextBlock Text="{Binding ConnectionStatus}" AutomationProperties.Name="Connection Status" />
                </StackPanel>
            </StatusBarItem>
            <Separator />
            <StatusBarItem HorizontalAlignment="Right"
                           Visibility="{Binding IsUpdateAvailable, Converter={StaticResource BoolToVisibilityConverter}}">
                <TextBlock Text="⬆ Update Available" Foreground="DodgerBlue" Cursor="Hand"
                           AutomationProperties.Name="Update Available" />
            </StatusBarItem>
        </StatusBar>
    </Grid>
</ui:FluentWindow>
