<UserControl x:Class="SysOpsCommander.App.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:SysOpsCommander.ViewModels;assembly=SysOpsCommander.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=vm:DashboardViewModel}"
             AutomationProperties.Name="Dashboard View">
    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Welcome / Domain Info -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Dashboard"
                       Style="{StaticResource SectionHeaderStyle}" />
            <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                <TextBlock Text="Welcome, " Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                <TextBlock Text="{Binding CurrentUserName}"
                           FontWeight="SemiBold"
                           AutomationProperties.Name="Current User Name" />
                <TextBlock Text="  |  Domain: " Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="8,0,0,0" />
                <TextBlock Text="{Binding ActiveDomainName}"
                           FontWeight="SemiBold"
                           AutomationProperties.Name="Active Domain Name" />
            </StackPanel>
        </StackPanel>

        <!-- Quick Connect Panel -->
        <Border Grid.Row="1"
                Background="{DynamicResource ControlFillColorDefaultBrush}"
                BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                BorderThickness="1" CornerRadius="8"
                Padding="16" Margin="0,0,0,16">
            <StackPanel>
                <TextBlock Text="Quick Connect"
                           FontSize="14" FontWeight="SemiBold"
                           Margin="0,0,0,8" />
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0"
                             Text="{Binding QuickConnectHostname, UpdateSourceTrigger=PropertyChanged}"
                             AutomationProperties.Name="Quick Connect Hostname" />
                    <Button Grid.Column="1"
                            Content="Connect"
                            Command="{Binding QuickConnectCommand}"
                            Margin="8,0,0,0"
                            MinWidth="80"
                            AutomationProperties.Name="Quick Connect Button" />
                </Grid>
                <TextBlock Text="{Binding QuickConnectResult}"
                           Margin="0,8,0,0"
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                           TextWrapping="Wrap"
                           AutomationProperties.Name="Quick Connect Result" />
            </StackPanel>
        </Border>

        <!-- Recent Executions -->
        <Border Grid.Row="2"
                Background="{DynamicResource ControlFillColorDefaultBrush}"
                BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                BorderThickness="1" CornerRadius="8"
                Padding="16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
                    <TextBlock Text="Recent Executions"
                               FontSize="14" FontWeight="SemiBold" />
                    <Button Content="Refresh"
                            Command="{Binding LoadRecentExecutionsCommand}"
                            Margin="16,0,0,0"
                            AutomationProperties.Name="Refresh Recent Executions" />
                </StackPanel>

                <DataGrid Grid.Row="1"
                          ItemsSource="{Binding RecentExecutions}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          SelectionMode="Single"
                          AutomationProperties.Name="Recent Executions Grid">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Time" Binding="{Binding Timestamp, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}" Width="140" />
                        <DataGridTextColumn Header="Script" Binding="{Binding ScriptName}" Width="*" />
                        <DataGridTextColumn Header="Targets" Binding="{Binding TargetHostCount}" Width="60" />
                        <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="90" />
                        <DataGridTextColumn Header="Duration" Binding="{Binding Duration, StringFormat='{}{0:mm\\:ss}'}" Width="80" />
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
